1768. Merge Strings Alternately

Used Two Pointers to merge two strings alternately.

i tracks word1 and j tracks word2.
Add one character from each string to result.
Continue while either string still has characters.
if conditions handle cases where one string is shorter.

Time: O(n + m)
Space: O(n + m) for the result