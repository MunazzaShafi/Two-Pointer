Used the array reversal technique with Two Pointers to rotate the array in-place.

Reverse the whole array.
Reverse the first k elements.
Reverse the remaining elements.
Use k % nums.length to handle large k.

Time: O(n)
Space: O(1)